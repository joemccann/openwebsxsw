<!DOCTYPE html>
<html lang="en">
<%- include head %>
<body>

  <noscript>
    <p>Oooooo, sorry that's the wrong answer. You actually need JavaScript enabled!</p>
    <p>Be sure and come back when you have JavaScript enabled - It's 2013!!!!111!1!</p>
  </noscript>

  <div id="container">
    <section role="main">
      <h2>Email List</h2>
      <div class="pad emailcontainer">
        
      <% if (emails.length) { %>
        <ul>
          <% emails.forEach(function(el,i){ var c = (i % 2) ? 'odd' : 'even'; %>
            <li class="<%- c %>"><%= el.name %> | <%= el.email %> | <a href="http://twitter.com/<%= el.twitter %>"><%= el.twitter %></a> | <%= el.klout %></li>
          <% }) %>
        </ul>
      <% } %>
      
      </div>      
    </section>
    <%- include footer %>
  </div>
  
<%- include scripts %>
</body>
</html>